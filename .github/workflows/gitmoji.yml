on:
  push:
  pull_request:
   types: [opened, synchronize]
   
jobs:
  check_pr_title:
    runs-on: ubuntu-latest
    steps:
    - name: Check PR title
      run: |
        title=${{ github.event.pull_request.title }}
        echo title
        if [[ ${title:0:1} != ":" ]]; then
          echo "El título del pull request no comienza con ':'"
          exit 1
        fi
