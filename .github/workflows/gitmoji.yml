on:
  push:
  pull_request:
   types: [opened, synchronize]
   
jobs:
  prod-check:
    if: ${{ github.event.pull_request.title.startsWith(:)}}
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
