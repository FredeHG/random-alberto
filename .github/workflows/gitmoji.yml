on:
  pull_request:
   
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Check for emojis
      run: |
        python3 -c "import emoji; print(emoji.emojize('Hello, 👋 World! 😃'))"
